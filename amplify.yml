version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd jamcraft-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: jamcraft-app/build
    files:
      - '**/*'
  cache:
    paths:
      - jamcraft-app/node_modules/**/*